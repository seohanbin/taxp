<h1>탑승 인원 확인</h1>

<h4>현재 <%= @selectedroute.id %>번 방의 등록 상태</h4>
<br>
	시작점 : <%= @selectedroute.startpoint %> <br>
	도착점 : <%= @selectedroute.endpoint %> <br>
	출발시간 : <%= @selectedroute.enrolltime.to_s[0,16] %> <br>
	등록 유저 수 : <%= @selectedroute.users.count %> <br>

<br>
<div class="well">
	<h4>현재 <%= @selectedroute.id %>번 방의 접속자 명단</h4>
	<% @selectedroute.users.each do |user| %>
		<%= user.email %> <br>
	<% end %>
</div>
<%#= link_to "리스트 다시보기", course_memberlist_path(startpoint: @selectedroute.startpoint, endpoint: @selectedroute.endpoint, enrolltime: Time.new(@selectedroute.enrolltime) ) %>
<p><%= link_to "코스 다시 선택하러 가기" , course_courselist_path,class: "btn btn-default" %> </p>
<p><%= link_to "시간 다시 선택하러 가기" , course_timelist_path(startingpoint: @selectedroute.startpoint, endingpoint: @selectedroute.endpoint),class: "btn btn-default" %> </p>
<h4>카카오톡 링크</h4>

<div class="well">
	<h4>현재 진행 단계</h4>
	<div class="list-group">
		<a  class="list-group-item">경로찾기</a>
		<a  class="list-group-item">시간대 선택</a>
		<a  class="list-group-item">코스 접속</a>
		<a  class="list-group-item active">탑승 인원 확인</a>
	</div>
</div>
